# Rime schema settings
# encoding: utf-8
schema:
  schema_id: wanxiang_reverse
  name: "万象：拆分与笔画反查"
  version: "LTS"
  author: amzxyz
  description: |
    万象的反查功能模块，方案融合了组字与笔画的能力
engine:
  processors:
    - key_binder
    - speller
    - selector
    - navigator
    - express_editor
  segmentors:
    - abc_segmentor
  translators:
    - echo_translator
    - table_translator
  filters:
    - uniquifier

key_binder:
  __include: default:/key_binder?


speller:
  alphabet: "abcdefghijklmnopqrstuvwxyz;"
  delimiter: " '"
  algebra:
    # __include: yuan_algebra:/reverse/全拼
    # __patch: yuan_algebra:/reverse/lssp      #笔画类型有zxcvb和lssp（适配李氏三拼）可选

translator:
  dictionary: wanxiang_reverse
  enable_user_dict: false